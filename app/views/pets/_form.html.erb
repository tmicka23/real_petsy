<%= simple_form_for pet do |form| %>

  <% if @pet.image? %>
  <p>
    <img src="<%= @pet.image_url %>" class="circle">
  </p>
  <% end %>
  <%= form.input :image, as: :file %>
  <%= form.input :name %>
  <%= form.association :species, include_blank: false %>
  <%= form.input :gender, collection: {Male: :M, Femelle: :F}, include_blank: false %>
  <%= form.input :birthday, as: :string, input_html: {class: 'datepicker'} %>
  <%= form.button :submit, pet.new_record? ? 'Créer' : 'Modifier', class: 'btn-primary' %>

<% end %>
