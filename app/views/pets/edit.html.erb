<% content_for :content do %>
  <div class="card">
    <div class="card-header">Modifier <%= @pet.name %></div>
    <div class="card-body">
      <%= render 'form', pet: @pet %>
    </div>
  </div>

<% end %>

<%= render template: 'layouts/profile' %>
