<p id="notice"><%= notice %></p>


<% if user_signed_in? %>
      <% if @pet.followedBy?(current_user) %>
        <%= link_to "Se désabonner", [@pet, Subscription], method: :delete, class: 'btn btn-warning' %>
      <% else %>
        <%= link_to "S'abonner", [@pet, Subscription], method: :post, class: 'btn btn-success' %>
      <% end %>
    <% end %>


<p>
  <strong>Name:</strong>
  <%= @pet.name %>
</p>
<% if @pet.image %>
  <p>
    <strong>Photo:</strong>
  <img src="<%= @pet.image_url(:large) %>" alt="<%= @pet.name %>">
</p>
<% end %>

<p>
  <strong>Gender:</strong>
  <%= @pet.gender %>
</p>

<p>
  <strong>Birthday:</strong>
  <%= @pet.birthday %>

</p>

<p>
  <strong>User:</strong>
  <%= @pet.user.username %>
</p>

<p>
  <strong>Species:</strong>
  <%= @pet.species.name %>
</p>

<%= link_to 'Retour', pets_path %>
